import styles from './Footer.module.scss'

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <span aria-hidden={true}>git:(main) ✓ nothing to commit</span>
    </footer>
  )
}

export default Footer
